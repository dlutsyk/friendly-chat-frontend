import { Logo } from "@ui";

export default function MobileHeader() {
    return (
        <header
            className="w-full bg-blue-500 text-white p-4 flex items-center justify-center"
        >
            <Logo />
        </header>
    )
}
